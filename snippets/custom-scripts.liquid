<script>
{%- comment -%}
window.addEventListener('DOMContentLoaded', () => {
  {% for item in checkout.line_items %}
  {% if item.sku == '6002793' %}
  if (document.querySelector('label[for="checkout_id_delivery-pickup"]') != null) {
    document.querySelector('label[for="checkout_id_delivery-pickup"]').parentElement.parentNode.remove();
  }
  {% endif %}
  {% endfor %}
});
{%- endcomment -%}
window.addEventListener('DOMContentLoaded', () => {
  if (document.querySelector('label[for="checkout_id_delivery-pickup"]') != null) {
    document.querySelector('label[for="checkout_id_delivery-pickup"]').parentElement.parentNode.remove();
  }
});
</script>